#pragma HLSL_Version 2018
#pragma ColorSpace Gamma
#pragma Key "Luminosity"
#pragma KeyName_jp "輝度"

#include "./BlendColor.hlsl"
float4 ColorBlend(float4 BaseColor, float4 AddColor)
{
  if (BaseColor.a <= 0.0)
  {
    return AddColor;
  }
  if (AddColor.a <= 0.0)
  {
    return BaseColor;
  }
  float3 BlendColor = LuminosityComposite(BaseColor.rgb, AddColor.rgb);
  return AlphaBlending(BaseColor, AddColor, BlendColor);
}
