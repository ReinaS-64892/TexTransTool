#pragma HLSL_Version 2018
#pragma ColorSpace Gamma
#pragma Key "Clip/VividLight"
#pragma KeyName_jp "クリスタ/ビビッドライト"

#include "./BlendColor.hlsl"
float4 ColorBlend(float4 BaseColor, float4 AddColor)
{
  if (BaseColor.a <= 0.0)
  {
    return AddColor;
  }
  if (AddColor.a <= 0.0)
  {
    return BaseColor;
  }
  float3 BlendColor = VividLightComposite(BaseColor.rgb, AddColor.rgb);
  return ClipAlphaBlending(BaseColor, AddColor, BlendColor);
}
